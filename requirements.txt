# sEMG lower-limb LOSO pipeline — Python 3.10
# Pin exact versions from your working .venv with:  pip freeze > requirements.lock.txt
# The packages below are the ones the pipeline imports; the version constraints
# are conservative minimums known to work.

numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
torch>=2.0          # CNN training (CUDA optional; CPU works for inference)
PyWavelets>=1.4     # db4 DWT wavelet-energy feature
joblib>=1.3
tqdm>=4.65
psutil>=5.9         # used by run_with_memory_guard.py (auto kill/restart on high memory)

# Document/figure tooling (used outside the ML pipeline)
python-docx>=1.1
openpyxl>=3.1
